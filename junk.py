 #for i in range(0, 4):
                #for j in range(0, 4):
                    # If we are not currently looking at the first row ...
                    # if i != 0:
                    #     print("Combined [" + str(i-1) + "][" + str(j) + "] & [" + str(i) + "][" + str(j) + "]")
                    #     board[i-1][j] += board[i][j]
                    #     board[i][j] = 0
                    #     PrintBoard(board)
                    
                    #if i > 0:
                    #print("else")
                    
                    
                        # notIsEmpty = i
                        # while notIsEmpty > 0 and board[notIsEmpty-1][j] == 0:
                        #     notIsEmpty -= 1

                        # print("Replaced [" + str(i-1) + "][" + str(j) + "] wiith [" + str(i) + "][" + str(j) + "]")
                        # board[notIsEmpty][j] = board[i][j]
                        # board[i][j] = 0

                        # if board[notIsEmpty-1][j] == board[notIsEmpty][j]:
                        #     board[notIsEmpty-1][j] += board[notIsEmpty][j]
                        #     board[notIsEmpty][j] = 0


                        # notIsEmpty = i
                        # while notIsEmpty > 0 and board[notIsEmpty-1][j] == 0:
                        #     notIsEmpty -= 1

                        # board[notIsEmpty][j] = board[i][j]
                        # board[i][j] = 0
                    
                    # Shift all non-zero numbers up